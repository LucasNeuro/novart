# Copie para `.env` na pasta `web/` e preencha com o projeto Supabase (Settings → API).
# Nunca commite `.env` — já está em `.gitignore`.

VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Produção (Render, etc.): URL pública https://sem-barra-no-fim
# Deve coincidir com o Site URL no Supabase (Auth → URL Configuration).
# VITE_APP_ORIGIN=https://seu-servico.onrender.com
